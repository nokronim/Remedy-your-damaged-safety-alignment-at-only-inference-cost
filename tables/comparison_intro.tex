% Comparison with existing defenses --- moved from Experiments to Introduction
% to motivate \method{}'s positioning early in the paper.
\begin{table}[t]
\caption{Comparison with existing defenses against fine-tuning-induced safety degradation. Columns: (1)~\textbf{Setup cost per checkpoint}~--- compute required to deploy the defense on each new fine-tuned model (training, projection, or just a forward pass); (2)~\textbf{Model-aware}~--- whether the defense is tailored to the specific fine-tuned target model rather than a one-size-fits-all classifier (the antonym of \textit{model-agnostic}); (3)~\textbf{Preserves model weights}~--- whether the deployed model weights are kept unchanged (avoiding any accuracy risk from weight modification); (4)~\textbf{Standalone at deploy}~--- whether deployment requires only the fine-tuned model itself, with no need for the original aligned base model $\mathcal{M}_0$ at inference time. \method{} is the only approach that is simultaneously model-aware, non-destructive, standalone, and requires only a single HyperNetwork forward pass per checkpoint. (\method{}'s one-time training phase uses $\mathcal{M}_0$ to construct $(\textsc{model}, \textsc{ssn})$ training pairs; at deployment, only the fine-tuned model and the trained HyperNetwork are needed.)}
\label{tab:comparison}
\centering
\renewcommand{\arraystretch}{1.25}
\resizebox{0.95\columnwidth}{!}{%
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Method} & \textbf{Setup cost} & \textbf{Model-} & \textbf{Preserves} & \textbf{Standalone} \\
                & \textbf{per checkpoint} & \textbf{aware}  & \textbf{model weights} & \textbf{at deploy} \\
\midrule
Vaccine~\cite{yi2024vaccine}             & full train     &  \checkmark &  $\times$  &  \checkmark \\
SafeInstr~\cite{bianchi2024safetytuned}  & partial train  &  \checkmark &  $\times$  &  \checkmark \\
Safe LoRA~\cite{hsu2024safe}             & projection     &  \checkmark &  $\times$  &  $\times$    \\
OneShot~\cite{zhang2026safety}           & 1-example SFT  &  \checkmark &  $\times$  &  \checkmark  \\
Llama Guard 4~\cite{inan2023llama}       & none           &  $\times$   &  \checkmark &  \checkmark  \\
WildGuard~\cite{han2024wildguard}        & none           &  $\times$   &  \checkmark &  \checkmark  \\
\midrule
\textbf{\method{} (ours)}                & 1 fwd pass     &  \checkmark &  \checkmark &  \checkmark  \\
\bottomrule
\end{tabular}%
}
\end{table}
