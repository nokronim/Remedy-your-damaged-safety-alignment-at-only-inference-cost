\section{Introduction}
\label{sec:intro}

% Fine-tuning a safety-aligned pre-trained LLM on a downstream task is the standard way to specialize open-source models, but it routinely degrades the safety alignment of the base model. Even when the fine-tuning data contains no harmful content, the harmful-response rate of a LoRA fine-tuned model can climb from under 5\% (the aligned baseline) to over 40\% on standard safety benchmarks~\cite{qi2024finetuning}, because refusal behavior is encoded in only a handful of shallow token-position subspaces~\cite{qi2025safety}, so even task-relevant gradient updates on benign data can shift the model outside the aligned region within tens of steps.

% % The scale of the problem is set by the deployment ecosystem: a large and rapidly growing population of fine-tuned checkpoints, each derived from the same handful of safety-aligned base models, but none of which carries a guarantee that those safety properties survive the fine-tuning recipe. Restoring safety on each checkpoint individually requires a per-model intervention; defending all of them with a single off-the-shelf classifier ignores recipe-specific shifts in the target model.




% \textbf{(i) Fine-tuning-stage methods} (Lisa~\cite{huang2024lisa}, Vaccine~\cite{yi2024vaccine}, RepNoise~\cite{rosati2024representation}, SafeInstr~\cite{bianchi2024safetytuned}, Constrained-SFT~\cite{qi2025safety}) treat safety preservation as an additional optimization constraint during fine-tuning. This \textit{forces a Pareto trade-off between downstream utility and residual harm}: tighter safety regularization sacrifices accuracy on the target task, looser regularization leaks harmful content. They also require coordination with the fine-tuner and cannot be retroactively applied to checkpoints that have already been released.

% \textbf{(ii) Post-fine-tuning weight-modification methods} project or merge the fine-tuned weights to recover alignment (Safe LoRA~\cite{hsu2024safe}, OneShot SFT~\cite{zhang2026safety}, Task Arithmetic~\cite{ilharco2023task}). These approaches \textit{modify the deployed weights}, which (a)~can degrade task accuracy because the modification induces forgetting of the fine-tuned task, (b)~require access to the original aligned base model at deployment time, and (c)~operate on the same Pareto trade-off as fine-tuning-stage methods.

% \textbf{(iii) Model-agnostic external classifiers} sit in front of the LLM and filter inputs/outputs (Llama Guard~\cite{inan2023llama}, ShieldGemma~\cite{zeng2024shieldgemma}, WildGuard~\cite{han2024wildguard}, AEGIS~\cite{ghosh2024aegis}, Granite Guardian~\cite{padhi2024granite}). Because they ignore the specific fine-tuning recipe, they \textit{miss recipe-specific failure modes} (e.g., jailbreaks that align with newly learned domain syntax) and waste capacity on out-of-distribution risks the deployed model never faces.

% Existing defenses thus force a choice between per-checkpoint training cost (families i and ii) and recipe-blind protection (family iii).

% %\paragraph{Our research question.}
% In this work, we pose and study the following question:
% \begin{quote}
% \textit{Is it possible to train a generalizable hypernetwork that, given any fine-tuned checkpoint, efficiently produces a lightweight side network restoring the safety behavior of the original aligned model --- without retraining, without modifying the deployed weights, and without sacrificing downstream accuracy?}
% \end{quote}

% %\paragraph{Our motivation.} 

% Our key insight is that the Pareto frontier between safety and utility exists \emph{only because the safety mechanism shares parameters with the task mechanism}. Delegating safety to a separate, conditionally engaged side network~\cite{sung2022lst} that runs in parallel with the frozen fine-tuned model breaks this coupling: (a)~the fine-tuned model retains its full task capacity, and (b)~the side network can be trained to restore refusal on harmful prompts. The ensemble inherits the utility of the fine-tuned model and the safety of the aligned base model --- removing the trade-off rather than balancing it.

% The remaining problem is cost: training a bespoke side network for every fine-tuned checkpoint is impractical at the scale of the adapter ecosystem. We address this by training a hypernetwork~\cite{ha2017hypernetworks, chauhan2024brief} that, given a small set of activation fingerprints from a new fine-tuned model, generates the side network's weights in a single forward pass --- no gradient updates, no safety data needed at deployment time. The hypernetwork is trained once and amortizes across all subsequent fine-tuned checkpoints.

% %\paragraph{\method{}.} 

% We introduce \textbf{\method{}} (Figure~\ref{fig:pipeline}), which makes this generation tractable through three design choices, each addressing a specific obstacle in mapping a fine-tuned checkpoint to safety weights:

% % \begin{enumerate}[leftmargin=*,topsep=2pt,itemsep=2pt]
% %     \item \textit{What signal to condition on.} Rather than embedding text descriptions of the fine-tuning recipe (which may be unknown at deployment), we condition on \textbf{activation fingerprints} --- a per-prompt summary of how the frozen model's intermediate representations have shifted under fine-tuning, computable in a single forward pass with no gradient computation.
% %     \item \textit{How to make high-dimensional weight generation stable.} Each side-network layer is generated from both the corresponding base-layer activation (depth-specific specialization) and a global mean across selected base layers (cross-layer coherence). This \textbf{local~+~global conditioning} provides both depth-specific specialization and cross-layer consistency in the generated weights.
% %     \item \textit{How to keep inference cheap.} A $\lambda$-classifier predicts a routing probability per prompt; if $\lambda < 0.5$ the base model runs as-is, and if $\lambda \geq 0.5$ a fixed refusal template is substituted. This \textbf{hard-threshold routing} eliminates decode-time overhead on safe prompts and yields a net speed-up on harmful ones.
% % \end{enumerate}

% \input{tables/comparison_intro}

% \begin{figure*}[t]
% \centering
% \includegraphics[width=\textwidth]{figures/assets/safety_remedy_figure.pdf}
% \caption{\method{} pipeline. \textbf{Stages 1--3 (training, one-time):} fine-tune the base model on each training domain (1), train a domain-specific \ssn{} per fine-tuned checkpoint (2), and train the hypernetwork to map activation fingerprints to \ssn{} weights (3). \textbf{Stage 4 (deployment):} for any new fine-tuned model, extract calibration activations and generate a model-specific \ssn{} in a single forward pass.}
% \label{fig:pipeline}
% \end{figure*}

% \paragraph{Contributions.}
% \begin{enumerate}[leftmargin=*,topsep=2pt,itemsep=2pt]
%     \item We formulate safety restoration as \textit{activation-conditioned generation}: the side network is automatically produced from each fine-tuned model's activation fingerprint, requiring neither per-model training nor safety data at deployment.
%     \item We design \method{}, combining layer-wise factorized weight generation with local+global activation conditioning to make high-dimensional weight prediction stable and generalizable.
%     \item We validate on two architectures (Qwen2-7B and LLaMA-3-8B): the hypernetwork is trained on 22 fine-tuning datasets and evaluated zero-shot on unseen fine-tuned checkpoints from three holdout splits of 4 held-out datasets each (12 held-out evaluations total), using three safety benchmarks (BeaverTails plus two fully zero-shot: AdvBench and HEx-PHI). \method{} reduces harmful response rates from 19--31\% to under 1\% on every held-out checkpoint while keeping task accuracy within one percentage point of the fine-tuned baseline on average.
% \end{enumerate}



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%% Revised introduction section by Bang

Fine-tuning safety-aligned LLMs is a common way to adapt them to downstream tasks, but it can also weaken their safety alignment. Prior work has shown that, even when the fine-tuning data is totally benign, the harmful-response rate of a LoRA fine-tuned model can climb from under 5\% (the aligned baseline) to over 40\% on standard safety benchmarks~\cite{qi2024finetuning}. One possible reason is that safety alignment can depend heavily on the model’s behavior in the first few output tokens~\cite{qi2025safety}. As a result, even benign fine-tuning updates may change the model’s refusal behavior and reduce its ability to reject harmful requests.


Existing defenses for this alignment degradation can be grouped into three main categories. The first category applies safety-preserving methods during fine-tuning. These methods add safety constraints during task adaptation, for example through regularization, safety data, or constrained optimization (Lisa~\cite{huang2024lisa}, RepNoise~\cite{rosati2024representation}, Constrained-SFT~\cite{qi2025safety}). They aim to preserve the base model's refusal behavior while learning the downstream task. However, they often introduce a trade-off between safety and utility: stronger safety constraints may reduce task performance, while weaker constraints may still allow harmful responses. They also require control over the fine-tuning process, so they cannot be easily applied to fine-tuned checkpoints that have already been released.

The second category modifies the model weights after fine-tuning. These methods try to recover safety by editing, projecting, or merging the fine-tuned weights (Safe LoRA~\cite{hsu2024safe}, OneShot SFT~\cite{zhang2026safety}, Task Arithmetic~\cite{ilharco2023task}). They are useful when the model has already been fine-tuned, but they directly change the deployed weights. As a result, their modification may cause forgetting on downstream task performance. They may also require access to the original aligned base model at deployment time, and they still face the same basic safety--utility trade-off as fine-tuning-stage methods.


The third category uses model-agnostic external classifiers. These methods place a separate safety model before or after the LLM to filter harmful inputs or outputs (Llama Guard~\cite{inan2023llama}, WildGuard~\cite{han2024wildguard}, Granite Guardian~\cite{padhi2024granite}). They are easy to deploy because they do not modify the target model or require access to its fine-tuning process. However, since they are not tailored to a specific fine-tuned model, they may miss failures caused by the particular fine-tuning recipe, such as harmful responses triggered by newly learned task-specific syntax or domain patterns. They may also spend capacity detecting risks that are not relevant to the deployed model.

Overall, existing defenses either require intervention during or after fine-tuning, which can be costly and may reduce task utility, or rely on model-agnostic filters that may not capture failures specific to a given fine-tuned model.

%% You may change model-specific to other similar terminology you prefered, and highlight it in the previous third category paragraph. 

Based on these limitations, we ask the following question:
\begin{quote}
\textit{Can we recover the safety behavior of the original aligned model for a given fine-tuned checkpoint using a model-specific lightweight post-hoc method, without retraining, modifying the deployed weights, or degrading downstream task performance?}
\end{quote}


This question leads to three design requirements. First, it should not require access to or control over the original fine-tuning process. Second, it should not modify the deployed fine-tuned weights, since doing so may damage the task behavior learned during fine-tuning. Third, it should be specific to the fine-tuned checkpoint, rather than relying on a fixed external classifier that ignores how the model was adapted. These requirements motivate the use of a model-conditioned side network. Instead of changing the fine-tuned model itself, we attach a lightweight safety side network that runs alongside the frozen model and decides whether a prompt should be answered or refused. Since the side network is separate from the task model, the fine-tuned model can preserve its downstream ability, while the side network focuses on recovering safety behavior. To avoid training a new side network for every released checkpoint, we use a hypernetwork to generate the side network weights from the fine-tuned model's activation fingerprints.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/assets/intro_fig.pdf}
\caption{TODO: caption for the introduction figure.}
\label{fig:intro}
\end{figure}

\input{tables/comparison_intro}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/assets/safety_remedy_figure.pdf}
\caption{\method{} pipeline. \textbf{Stages 1--3 (training, one-time):} fine-tune the base model on each training domain (1), train a domain-specific \ssn{} per fine-tuned checkpoint (2), and train the hypernetwork to map activation fingerprints to \ssn{} weights (3). \textbf{Stage 4 (deployment):} for any new fine-tuned model, extract calibration activations and generate a model-specific \ssn{} in a single forward pass.}
\label{fig:pipeline}
\end{figure*}

We introduce \method{}, a post-hoc framework for restoring safety in fine-tuned LLMs without retraining or modifying the deployed model weights. Given a fine-tuned checkpoint, \method{} first extracts layer-wise activation fingerprints from a small set of calibration prompts. A hypernetwork then maps these fingerprints to generate the weights of a lightweight safety side network. The side network is then attached to the frozen fine-tuned model and produces a prompt-level safety score. At inference time, prompts predicted to be harmful are routed to a refusal response, while safe prompts are handled by the original fine-tuned model. In this way, \method{} is checkpoint-specific, because the generated side network depends on the target model's activations; post-hoc, because it is applied after fine-tuning; and non-invasive, because it does not update or edit the deployed model weights.

Our contributions are as follows:
\begin{enumerate}[leftmargin=*,topsep=2pt,itemsep=2pt]
    \item We formulate post-hoc safety restoration as activation-conditioned side network generation. Given a fine-tuned checkpoint, our method generates a model-specific safety side network from its activation fingerprints, without additional gradient updates or safety data at deployment time.

    \item We design \method{}, a hypernetwork-based framework that predicts the weights of a lightweight safety side network. To make this prediction feasible, \method{} uses layer-wise activation conditioning, direction-magnitude decomposition, and factorized weight generation.

    \item We show that the generated side network can restore refusal behavior while preserving downstream task performance. Across Qwen2-7B and LLaMA-3-8B, the hypernetwork is trained on fine-tuned models from training domains and evaluated zero-shot on held-out fine-tuned checkpoints. On three safety benchmarks, \method{} reduces harmful response rates from 19--31\% to below 1\% on every held-out checkpoint, while keeping downstream task accuracy within 1\% of the fine-tuned baseline on average.
\end{enumerate}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%